# Security and privacy

Money Tracker has no backend and stores data in browser `localStorage`. Backups remain
the user's responsibility. Restore files are schema-validated and capped at 10,000 rows,
but users should import only backups they trust.

Report vulnerabilities privately through GitHub Security Advisories. Do not attach real
financial records to an issue.
